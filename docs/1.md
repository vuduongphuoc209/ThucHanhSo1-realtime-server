PHASE 9 — Chuẩn bị Backend

Bây giờ mới bắt đầu phần quan trọng: RESTful API + WebSocket.

Cấu trúc backend:

advanced-web-chat/
│
├── client/
│ └── React + TypeScript
│
└── server/
└── Node.js + Express + TypeScript

Tạo backend:

cd ..

mkdir server
cd server

npm init -y

Cài dependencies:
npm install express cors dotenv mongoose
npm install bcryptjs jsonwebtoken
npm install socket.io

Dev dependencies:
npm install -D typescript ts-node-dev
npm install -D @types/node
npm install -D @types/express
npm install -D @types/cors
npm install -D @types/bcryptjs
npm install -D @types/jsonwebtoken

Khởi tạo TypeScript:
npx tsc --init

Cấu trúc Backend
server/
│
├── src/
│ ├── config/
│ │ └── database.ts
│ │
│ ├── controllers/
│ │ ├── auth.controller.ts
│ │ ├── user.controller.ts
│ │ ├── conversation.controller.ts
│ │ └── message.controller.ts
│ │
│ ├── middleware/
│ │ ├── auth.middleware.ts
│ │ └── error.middleware.ts
│ │
│ ├── models/
│ │ ├── User.ts
│ │ ├── Conversation.ts
│ │ └── Message.ts
│ │
│ ├── routes/
│ │ ├── auth.routes.ts
│ │ ├── user.routes.ts
│ │ ├── conversation.routes.ts
│ │ └── message.routes.ts
│ │
│ ├── socket/
│ │ └── socket.ts
│ │
│ ├── types/
│ │
│ ├── app.ts
│ └── server.ts
│
├── .env
├── .gitignore
├── package.json
└── tsconfig.json

3. Kết nối MongoDB
4. Tạo Express App
5. Tạo Server
   dùng http.createServer(app)
   thay vì chỉ:app.listen()
   vì sau này chúng ta sẽ gắn Socket.IO trực tiếp vào HTTP server này.
6. Sửa package.json
