PHASE 12 — JWT Middleware + /me + kết nối React Login/Register
React Register
↓
POST /api/auth/register
↓
MongoDB Atlas
↓
JWT Token
↓
React lưu authentication state

React
↓ Authorization: Bearer <token>
GET /api/auth/me
↓
JWT Middleware
↓
MongoDB
↓
User
